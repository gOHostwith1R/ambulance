import styled from 'styled-components';
import { ForgotPasswordProps } from './types';

export const ForgotPasswordStyled = styled.div <ForgotPasswordProps>`
  margin: 32px 0 0 0;
  display: inline-block;
`;
