import styled from 'styled-components';

export const TimeSlotContainerStyled = styled.div`
  display: grid;
  gap: 16px;
  position: relative;
  max-width: 364px;
  grid-template-columns: repeat(auto-fill, max(104px));
  `;
