export const VALIDATION_EMAIL = /^([a-z0-9_-]+\.)*[a-z0-9_-]+@[a-z0-9_-]+(\.[a-z0-9_-]+)*\.[a-z]{2,6}$/i;

export const STATUS_PATIENT = {
    confirmed: 'confirmed',
    canceled: 'canceled',
    waiting: 'waiting',
}