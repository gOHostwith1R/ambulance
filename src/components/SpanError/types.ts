import React from 'react';

export interface SpanErrorProps {
  children?: React.ReactNode | JSX.Element,
  variant: string,
  role?: string,
}

export interface SpanErrorStyledProps {
  variant: string,
}
