import React from 'react';
import { FormStyled } from '../Form/form.styled';

const FormRestoreMessage = ({ children }) => (
  <FormStyled>
    { children }
  </FormStyled>
);

export default FormRestoreMessage;
